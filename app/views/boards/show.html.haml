.container
  %h2.card_board_pageTitle= @board.name
  - @tasks.each do |task|
    = link_to board_task_path(@board,task) do
      .card_content
        -if task.eyecatch.attached?
          .card_image
            = image_tag task.eyecatch
        .card_task
          .card_task_name
            = task.name
          .card_task_term
            term #{task.term}
        .card_description
          = task.description
        .card_detail
          .card_avatar
          = image_tag task.user.avatar_image, class: 'avatar'
          .card_detail_commentIcon
            %span 22
            -# コメント数を取得して表示する
            =image_tag 'comment-count.svg'
        .card_auther_name
          - if task.user.present?
            %p= task.user.display_name

  = link_to new_board_task_path(@board) do
    .card_add_new
      + Add new card